const NODE_ENV = {
  DEV: 'DEV',
  PRO: 'PRO',
};

const LIMIT = {
  _5_MB: '5mb',
};

const REQUEST = {
  _WINDOW_MS: 1 * 60 * 1000,
  _MAX: 100,
};

module.exports = {
  NODE_ENV,
  LIMIT,
  REQUEST,
};
